// Evaluar con el operador ternario

const autenticado = true;
const puedePagar = true;

console.log(autenticado && puedePagar ? 'Si puede Pagar' : 'No esta autenticado')
// El ? es en caso de que se cumpla, y el : en caso de que no