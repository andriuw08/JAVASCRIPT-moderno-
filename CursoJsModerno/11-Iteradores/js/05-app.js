// do while se ejecuta minimo 1 vez y luego verifica

let i = 0; // Inicio

do {
    console.log(`Numero ${i}`)
    i++; // Incremento
} while (i < 10); // Condicion

// Siempre se enjecuta al menos una vez, asi sea que no se cumpla la condicion