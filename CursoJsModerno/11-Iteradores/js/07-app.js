// forOf

const pendientes = ['Tarea', 'Comer', 'Entrenar']

for ( let pendiente of pendientes ) {
    console.log(pendiente)
}
// es como un for pero lo que hace es ir recorriendo cada uno de los elementos de pendientes y asignando a un pendienteç

