// querySelectorAll retorna todos los elementos con la condicion, no solo el primero

const card = document.querySelectorAll('.card');
console.log(card) // Va a retornar todos los card, no solo el primero