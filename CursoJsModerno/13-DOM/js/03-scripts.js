// Seleccionar elementos por su ID

const formulario = document.getElementById('formulario')
console.log(formulario)

// Si hay dos con el mismo id, va a seleccionar solo el primer elemento y es el que va a devolver
