const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    disponible: true,
}

console.log(Object.keys( producto ) );
//Retorna las llaves de los objetos

console.log(Object.values( producto ) );
//Retorna los valores de los objetos

console.log(Object.entries( producto ) );
//Retorna todo de los objetos
