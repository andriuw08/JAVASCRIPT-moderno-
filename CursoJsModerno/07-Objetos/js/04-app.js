const producto = {
    nombre: "Monitor 20 pulgada",
    precio: 300,
    disponible: true,
}

//Extraer valores de un objeto
const {nombre} = producto;
// En las llaves coloco lo que quiero extraer y crea la variable con el mismo nombre en el mismo paso

console.log(nombre);
